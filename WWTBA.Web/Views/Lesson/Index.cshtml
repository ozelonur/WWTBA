@{
    ViewData["Title"] = "Index";
}
@model List<LessonDto>

<div style="display: flex; justify-content: center;">
    <table class="table table-striped" style="width: auto;">
        <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Actions</td>
        </tr>
        </thead>
        
        <tbody>
        @foreach (LessonDto item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    <a class="btn btn-warning" asp-controller="Lesson" asp-action="Update" asp-route-id="@item.Id">Update</a>
                    <a class="btn btn-danger" asp-controller="Lesson" asp-action="Remove" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
